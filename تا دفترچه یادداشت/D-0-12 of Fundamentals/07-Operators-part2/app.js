// Operators
let x = 20
let y = 10
let z = '20'
let m;

// Arithmetic 
m = x + y // 30
m = x - y // 10
m = x * y // 200
m = x / y  // 2
m = x % y  // 0

// Assignment  
x += y // x = x + y
x -= y // x = x - y
x *= y // x = x * y
x /= y // x = x / y
x %= y // x = x % y


// Comparison  // true // false
x = 20
m = x == y
m = x == z
m = x === z
m = x != y
m = x !== z
m = x >= y
m = x <= y


// Logical
m = (x > y && x > 5) // true & true ==> true
m = (x < y && x > 5) // false & true ==> false
m = (x < y && x < 5) // false &, fasle ==> false

m = (x > y || x > 5) // true or true ==> true
m = (x < y || x > 5) // false or true ==> true
m = (x < y || x < 5) // false or fasle ==> false






console.log(m)
console.log(x)
console.log(y)
console.log(z)